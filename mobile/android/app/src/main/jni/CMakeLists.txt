cmake_minimum_required(VERSION 3.13)

project(zaycomm_appmodules)

# React Native 0.86's standard application CMake entry point.
# This wires ReactAndroid, generated autolinking, and the New Architecture
# native targets into the Android application build.
include(${REACT_ANDROID_DIR}/cmake-utils/ReactNative-application.cmake)
